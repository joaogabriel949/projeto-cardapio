name: goomer_nutri
description: "Gestão Nutricional"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  path: ^1.9.1
  path_provider: ^2.1.5
  sqflite: ^2.4.2+1
  openfoodfacts: ^3.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/database/taco_alimentos.json
